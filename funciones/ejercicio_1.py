"""
Enunciado del problema:

Escribe una función en Python llamada `calcular_promedio` que reciba una lista de números enteros y devuelva el promedio de esos números. La función debe manejar correctamente listas vacías devolviendo `None` en ese caso.

Además, escribe una función llamada `main` que solicite al usuario ingresar una lista de números separados por comas, use la función `calcular_promedio` para calcular el promedio de esos números, y luego imprima el resultado.

Ejemplo de uso:
- Entrada: 1, 2, 3, 4, 5
- Salida: El promedio es 3.0

- Entrada: (lista vacía)
- Salida: No se puede calcular el promedio de una lista vacía.
"""